// Sound effect logic (placeholder)
export function playSound(type: 'sold' | 'bid' | 'trivia') {
  // Use a library or HTMLAudioElement for real implementation
  // Example: new Audio('/assets/sold.mp3').play();
  console.log(`Sound played: ${type}`);
}
